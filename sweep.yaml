method: grid
count: 1
metric:
  name: val_loss
  goal: minimize

parameters:
  batch_size:
    values: [32]

  epochs:
    value: 3

  learning_rate:
    value: 0.001

  dropout:
    values: [0.3]

  model_architecture:
    values: ["Net"]
