method: bayes

metric:
  name: val_loss
  goal: minimize

parameters:
  batch_size:
    values: [16]

  epochs:
    value: 5

  learning_rate:
    distribution: log_uniform
    min: 1e-4
    max: 2e-3

  model_architecture:
    values: ["ComplexNet"]
